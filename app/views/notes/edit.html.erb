<main class="container">
  <%= link_to sanitize("&larr; Back to notes"), notes_path %>
  <div class="header">
    <h1>Edit note</h1>
  </div>
<%= turbo_frame_tag "note_#{@note.id}" do%>
  <%= render "form", note: @note %>
  <%end%>
</main>